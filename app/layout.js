export const metadata = { title: 'CarBase Academy', description: 'AI-ассистент детейлинг центра' };
export default function RootLayout({ children }) {
  return <html lang="ru"><body style={{margin:0,padding:0}}>{children}</body></html>;
}
